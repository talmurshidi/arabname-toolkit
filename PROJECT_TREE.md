<!--
  AUTO-GENERATED — DO NOT EDIT BY HAND.
  Generated by `npm run docs:tree`. Verified up to date by
  `npm run docs:tree:check`, which is part of `npm run verify`.
  If this file is out of date, run `npm run docs:tree` and commit the
  result — do not edit this file directly, your changes will be
  overwritten the next time it regenerates.
-->

# Project Tree

## Directory tree

```
src/
├── core/
│   └── transliteration/
│       ├── brill/
│       │   ├── bracketArticleFix.ts
│       │   ├── consonantTable.ts
│       │   ├── consonantTemplates.ts
│       │   ├── engine.ts
│       │   ├── helpers.ts
│       │   ├── index.ts
│       │   ├── irregularRules.ts
│       │   ├── nameOrder.ts
│       │   └── sunLetterAssimilation.ts
│       ├── arabicHouseStyle.ts
│       ├── arabicToLatin.ts
│       ├── bracketSanitizer.ts
│       ├── corrections.ts
│       ├── dictionary.ts
│       ├── dictionaryMatcher.ts
│       ├── din31635.ts
│       ├── index.ts
│       ├── legacyEngine.mjs
│       └── orthographyNormalization.ts
├── services/
│   ├── BatchService.ts
│   ├── DictionaryService.ts
│   ├── HistoryService.ts
│   └── TransliterationService.ts
├── shared/
│   ├── types.ts
│   └── utils.ts
├── ui/
│   ├── components/
│   │   ├── AdvancedOptions.tsx
│   │   ├── Footer.tsx
│   │   ├── Header.tsx
│   │   └── VirtualKeyboard.tsx
│   ├── hooks/
│   │   └── useLocale.ts
│   ├── i18n/
│   │   ├── ar.ts
│   │   ├── en.ts
│   │   ├── index.ts
│   │   └── types.ts
│   └── pages/
│       ├── BatchPage.tsx
│       ├── ConverterPage.tsx
│       ├── DictionaryPage.tsx
│       └── MethodologyPage.tsx
├── App.tsx
├── index.css
├── main.tsx
└── vite-env.d.ts
tests/
├── core/
│   ├── alifWasla.test.ts
│   ├── arabicHouseStyle.test.ts
│   ├── arabicToLatin.test.ts
│   ├── bracketSanitizer.test.ts
│   ├── brillEngineParity.test.ts
│   ├── commonNameAliases.test.ts
│   ├── curlyQuoteNormalization.test.ts
│   ├── din31635.test.ts
│   ├── divineNames.test.ts
│   ├── muslimHeritageFixtures.test.ts
│   ├── orthographyNormalization.test.ts
│   ├── transliteration.test.ts
│   └── transliterationCorrections.test.ts
└── services/
    ├── batchService.test.ts
    ├── dictionaryService.test.ts
    ├── historyService.test.ts
    └── transliterationService.test.ts
scripts/
├── find-degraded-transliterations.ts
├── generate-project-tree.ts
└── release.ts
docs/
├── adr/
│   ├── 0001-react-spa.md
│   ├── 0002-single-page-app.md
│   ├── 0003-legacy-engine-preserved.md
│   ├── 0004-localstorage-only.md
│   ├── 0005-papaparse-lazy-import.md
│   ├── 0006-tailwind-design-system.md
│   ├── 0007-din31635-support.md
│   ├── 0008-orthography-fixes.md
│   ├── 0009-reverse-scheme-and-history.md
│   └── 0010-retire-legacy-engine.md
├── design/
│   ├── logo/
│   │   ├── logo-concept-a.png
│   │   ├── logo-concept-b.png
│   │   └── logo-icon-final.png
│   ├── manuscript-scholarly-system.md
│   └── stitch-brief.md
├── reference/
│   ├── batch-file-format.md
│   └── degraded-transliteration-candidates.md
├── ARCHITECTURE.md
└── METHODOLOGY.md
public/
├── favicon.png
├── logo.png
└── manifest.json
```

## Per-file function map

### `src/core/transliteration/brill/bracketArticleFix.ts`

- `function fixAlDashAndBracket(txt: string): string` — Normalizes trailing/bracketed "al-" reordering, then applies the general Brill-character fixups and capitalizes the result's first letter.
- `function isIncludeBracket(txt: string): boolean` — True if `txt` is wrapped in (or starts/ends with) parentheses.
- `function isIncludeDashAtEnd(txt: string): boolean` — True if `txt` ends with ", al-" (or a bracket/space variant) — the "trailing article" pattern.

### `src/core/transliteration/brill/consonantTable.ts`

The Brill consonant table, as {latin unit, Arabic base letter} pairs.

- `interface ConsonantUnit` — The Brill consonant table, as {latin unit, Arabic base letter} pairs.
- `const GEMINATION_DIGRAPHS: ConsonantUnit[]`
- `const GEMINATION_LETTERS: ConsonantUnit[]`
- `const SINGLE_DIGRAPHS: ConsonantUnit[]`
- `const SINGLE_LETTERS: ConsonantUnit[]`

### `src/core/transliteration/brill/consonantTemplates.ts`

- `function applyGeminationTable(txt: string, table: ConsonantUnit[]): string` — Applies the geminated-consonant template for every unit in `table`, in order.
- `function applySingleTable(txt: string, table: ConsonantUnit[]): string` — Applies the single-occurrence consonant template for every unit in `table`, in order.

### `src/core/transliteration/brill/engine.ts`

- `function applyArabicHarakatTransliteration(txt: string): string` — Applies `fixAlDashAndBracket`/`fixBrillChar` normalization, then converts each space-separated word of `txt` independently via `transliterationToArabicHarakat`.
- `function transliterationToArabic(txt: string): string` — Converts Brill Latin text to Arabic script without diacritics (ḥarakāt stripped).
- `function transliterationToArabicHarakat(txt: string): string` — Convert one already-space-free Brill Latin token to Arabic with full diacritics (ḥarakāt).

### `src/core/transliteration/brill/helpers.ts`

Small Brill-input normalization helpers.

- `function capitalizeFirstLetter(value: string): string` — Capitalizes the first letter, skipping past a leading ʿ/ʾ so e.
- `function fixBrillChar(txt: string): string` — Small Brill-input normalization helpers.
- `function removeHarakat(txt: string): string` — Strips all Arabic harakat (and any other non-letter/digit characters) from Arabic script text.

### `src/core/transliteration/brill/index.ts`

- `function applyArabicHarakatTransliteration(txt: string): string` — Applies `fixAlDashAndBracket`/`fixBrillChar` normalization, then converts each space-separated word of `txt` independently via `transliterationToArabicHarakat`.
- `function fixAlDashAndBracket(txt: string): string` — Normalizes trailing/bracketed "al-" reordering, then applies the general Brill-character fixups and capitalizes the result's first letter.
- `function replaceFilterNameOrder(txt: string): string` — Strips diacritics and special marks from a Brill Latin name to produce a plain-ASCII sort/index key (e.
- `function transliterationToArabic(txt: string): string` — Converts Brill Latin text to Arabic script without diacritics (ḥarakāt stripped).
- `function transliterationToArabicHarakat(txt: string): string` — Convert one already-space-free Brill Latin token to Arabic with full diacritics (ḥarakāt).

### `src/core/transliteration/brill/irregularRules.ts`

Rule blocks that don't fit the regular gemination/single-occurrence consonant templates (see consonantTemplates.

- `function applyCommonWords(txt: string): string` — Rule blocks that don't fit the regular gemination/single-occurrence consonant templates (see consonantTemplates.
- `function applyGlideExtras(txt: string): string` — The irregular w/y/ī/ū "One letter" rules: glide-vowel fusions (ūwa/ūwu/ūwi) and hamza+glide fusions (ʾy, ʾī, ʾū) that don't fit the clean single-occurrence template, plus the four glide/long-vowel letters' own single-occurrence rules (w, y, ī, ū — each ends with a trailing-sukūn strip, same as the templated letters).
- `function applyHamzaEarly(txt: string): string` — Early hamza-cluster rules (aʾū, uʾa, aʾ, uʾ, iʾ$, āʾa$, ʾa$, ʾa, ʾu, ʾi).
- `function applyHamzaLateAndVowelFallback(txt: string): string` — Late hamza/vowel fallback rules — applied after every consonant+vowel template has already run, so these only ever match a leading/bare vowel letter that wasn't already consumed as part of a consonant cluster (e.

### `src/core/transliteration/brill/nameOrder.ts`

Strips diacritics and special marks from a Brill Latin name to produce a plain-ASCII sort/index key (e.

- `function replaceFilterNameOrder(txt: string): string` — Strips diacritics and special marks from a Brill Latin name to produce a plain-ASCII sort/index key (e.

### `src/core/transliteration/brill/sunLetterAssimilation.ts`

Forward-direction sun-letter shadda assimilation: for a word beginning with the definite article "ال", adds a shadda to the following sun letter's short-vowel mark (ت ث د ذ ر ز س ش ص ض ط ظ ل ن — all fourteen).

- `function applySunLetterAssimilation(txt: string): string` — Forward-direction sun-letter shadda assimilation: for a word beginning with the definite article "ال", adds a shadda to the following sun letter's short-vowel mark (ت ث د ذ ر ز س ش ص ض ط ظ ل ن — all fourteen).

### `src/core/transliteration/arabicHouseStyle.ts`

- `function applyYaSukunHouseStyle(arabicText: string): string` — Enforces the yā-sukūn house style on already-transliterated Arabic text: a long ī (ي preceded by kasra, not part of an ay diphthong, and not itself followed by another vowel mark — i.

### `src/core/transliteration/arabicToLatin.ts`

Reverse direction: fully-diacritised Arabic script -> Brill Latin.

- `function generateLatinFromArabic(value: string): string`
- `function looksFullyDiacritized(arabicText: string): boolean` — True when the (whitespace-trimmed) Arabic text is diacritised precisely to the standard this project's own forward engine (`brill/`, ported from the retired `legacyEngine.

### `src/core/transliteration/bracketSanitizer.ts`

- `interface BracketFix`
- `interface BracketFixOptions`
- `const DEFAULT_BRACKET_FIX_OPTIONS: BracketFixOptions`
- `function sanitizeBrackets(input: string, options?: Partial<BracketFixOptions>): { text: string; fixes: BracketFix[] }` — Repairs stray/unmatched brackets (`(`, `[`, `{` with no counterpart anywhere in the string) by stripping the orphan character only.

### `src/core/transliteration/corrections.ts`

Exact-match corrections for source data entered without proper diacritic support.

- `const TRANSLITERATION_CORRECTIONS: Record<string, string>` — Exact-match corrections for source data entered without proper diacritic support.

### `src/core/transliteration/dictionary.ts`

- `const DICTIONARY_BY_FIRST_WORD: Map<string, DictionaryCandidate[]>`
- `interface DictionaryCandidate`
- `const TRANSLITERATION_DICTIONARY: Record<string, string>`

### `src/core/transliteration/dictionaryMatcher.ts`

- `interface DictionaryMatchResult`
- `interface DictionaryToken`
- `function dictionaryTransliterate(text: string): DictionaryMatchResult` — Greedy longest-match-first scan over word tokens against the TRANSLITERATION_DICTIONARY.
- `function reassembleDictionaryTokens(result: DictionaryMatchResult, transformUnmatched: (word: string) => string): string` — Reassembles tokens into a single string, applying `transformUnmatched` to any token that was not a dictionary hit (e.

### `src/core/transliteration/din31635.ts`

DIN 31635 ⇄ Brill conversion layer.

- `function brillToDin(text: string): string` — Convert Brill-spelled Latin text (as produced/accepted by this project's engine) to DIN 31635 spelling, for display alongside the Brill form.
- `function containsDin31635Chars(text: string): boolean` — True if the text contains any character that is specific to DIN 31635 (not shared with Brill).
- `function dinToBrill(text: string): string` — Convert DIN 31635-spelled Latin text to Brill-spelled Latin, so it can be fed into the existing Brill → Arabic pipeline unchanged.

### `src/core/transliteration/index.ts`

- `function applyAssimilatedArticleRule(text: string): string`
- `function applyElidedArticleRule(text: string): string` — Brill sources sometimes spell the definite article, after a preceding vowel, in its elided pronunciation form — "l-" or "'l-" (the apostrophe standing in for the silent hamzat al-waṣl) — e.
- `function applyIbnAlifRule(text: string): string` — Word-position-aware orthographic normalisations, applied to Brill Latin input _before_ dictionary matching and the Brill engine run.
- `interface BracketFix`
- `interface BracketFixOptions`
- `function brillToDin(text: string): string` — Convert Brill-spelled Latin text (as produced/accepted by this project's engine) to DIN 31635 spelling, for display alongside the Brill form.
- `function containsDin31635Chars(text: string): boolean` — True if the text contains any character that is specific to DIN 31635 (not shared with Brill).
- `function createNameOrder(value: string): string` — Build an ASCII sort key for a transliterated name.
- `const DEFAULT_BRACKET_FIX_OPTIONS: BracketFixOptions`
- `const DICTIONARY_BY_FIRST_WORD: Map<string, DictionaryCandidate[]>`
- `interface DictionaryCandidate`
- `interface DictionaryMatchResult`
- `interface DictionaryToken`
- `function dinToBrill(text: string): string` — Convert DIN 31635-spelled Latin text to Brill-spelled Latin, so it can be fed into the existing Brill → Arabic pipeline unchanged.
- `function generateArabic(value: string, options?: TransliterationOptions): string` — Generate Arabic without diacritics from a Brill-transliterated name.
- `function generateArabicHarakat(value: string, options?: TransliterationOptions): string` — Generate Arabic with diacritics from a Brill-transliterated name.
- `function generateLatin(value: string): string` — Reverse direction: convert fully-diacritised Arabic script to Brill Latin.
- `function generateTransliteration(value: string, options?: TransliterationOptions): TransliterationResult` — Full conversion: returns all four derived forms in one call.
- `function looksFullyDiacritized(arabicText: string): boolean` — True when the (whitespace-trimmed) Arabic text is diacritised precisely to the standard this project's own forward engine (`brill/`, ported from the retired `legacyEngine.
- `function normalizeTransliteratedName(value: string, options?: TransliterationOptions): string` — Normalise trailing ", al-" / bracket patterns in a transliterated name.
- `function sanitizeBrackets(input: string, options?: Partial<BracketFixOptions>): { text: string; fixes: BracketFix[] }` — Repairs stray/unmatched brackets (`(`, `[`, `{` with no counterpart anywhere in the string) by stripping the orphan character only.
- `function stripHarakatDiacritics(text: string): string`
- `const TRANSLITERATION_DICTIONARY: Record<string, string>`
- `const TRANSLITERATION_RULE_VERSION` — Bumped whenever the rules change in a way that would alter output for any existing input.
- `interface TransliterationOptions`
- `interface TransliterationResult`

### `src/core/transliteration/legacyEngine.mjs`

(Legacy character-level Brill engine — not parsed; treat rules as load-bearing.)

### `src/core/transliteration/orthographyNormalization.ts`

Word-position-aware orthographic normalisations, applied to Brill Latin input _before_ dictionary matching and the Brill engine run.

- `function applyAssimilatedArticleRule(text: string): string`
- `function applyElidedArticleRule(text: string): string` — Brill sources sometimes spell the definite article, after a preceding vowel, in its elided pronunciation form — "l-" or "'l-" (the apostrophe standing in for the silent hamzat al-waṣl) — e.
- `function applyIbnAlifRule(text: string): string` — Word-position-aware orthographic normalisations, applied to Brill Latin input _before_ dictionary matching and the Brill engine run.

### `src/services/BatchService.ts`

- `interface BatchOptions`
- `function exportReportToCsv(report: BatchReport): string` — Export a BatchReport to CSV string with columns: row, input, arabic_harakat, arabic, name_order, status, error
- `function previewCsvFile(file: File, options?: Pick<BatchOptions, 'column' | 'delimiter' | 'hasHeader'>): Promise<BatchPreview>` — Cheap upfront look at a CSV/TSV file, meant to be called as soon as the user selects a file (and again if they change the column/hasHeader options), so the UI can show a preview and surface validation problems _before_ the user commits to processing the whole file.
- `function processCsvFile(file: File, options?: BatchOptions): Promise<BatchReport>` — Process a CSV/TSV File object and return a BatchReport.

### `src/services/DictionaryService.ts`

- `const dictionaryService`
- `class DictionaryService`
  - `constructor(options?: { storageKey?: string })`
  - `add(entry: Omit<UserDictionaryEntry, 'id' | 'createdAt' | 'arabic'>): UserDictionaryEntry` — Add a new user entry.
  - `update(id: string, patch: Partial<Omit<UserDictionaryEntry, 'id' | 'createdAt'>>): UserDictionaryEntry` — Update an existing entry by ID.
  - `remove(id: string): boolean` — Remove an entry by ID.
  - `lookup(latin: string): UserDictionaryEntry | undefined` — Look up a Latin string (exact, case-insensitive).
  - `getAll(): UserDictionaryEntry[]` — Return all user entries sorted by Latin form.
  - `exportJson(): string` — Export all entries as a JSON string.
  - `importJson(json: string): number` — Import entries from a JSON string.

### `src/services/HistoryService.ts`

- `const historyService`
- `class HistoryService`
  - `constructor(options?: { storageKey?: string; maxEntries?: number })`
  - `push(output: ConversionOutput): HistoryEntry` — Add a conversion result to the history (most recent first).
  - `getAll(): HistoryEntry[]` — Return all stored entries (most recent first).
  - `remove(id: string): boolean` — Remove a single entry by ID.
  - `clear(): void` — Clear all history.

### `src/services/TransliterationService.ts`

- `const transliterationService` — Singleton instance for use across the UI layer.
- `class TransliterationService`
  - `convert(input: ConversionInput, options?: TransliterationOptions): ConversionOutput` — Convert a single name (Brill or DIN 31635, per `input.
  - `convertMany(inputs: ConversionInput[], options?: TransliterationOptions): Array< { input: ConversionInput; output: ConversionOutput } | { input: ConversionInput; error: string } >` — Convert multiple inputs.
  - `convertReverse(text: string, preferredScheme?: TransliterationScheme): ConversionOutput` — Convert fully-diacritised Arabic script to Latin (reverse direction).
  - `getRuleVersion(): string` — Returns the current rule version string.
  - `generateResultId(): string` — Generate a stable opaque ID for a conversion result.

### `src/shared/types.ts`

Types shared across all layers (core, services, ui).

- `interface BatchPreview` — Cheap upfront look at a CSV/TSV file: header row (if any), a handful of sample data rows, the resolved column index, the total data-row count, and any validation warnings — all computed from a single parse of the whole file, before the user commits to processing it.
- `interface BatchPreviewWarning` — A non-fatal issue found while previewing a batch file, surfaced to the user before they commit to processing the whole file.
- `interface BatchReport`
- `interface BatchRow`
- `type BatchRowResult`
- `type ConversionDirection`
- `interface ConversionInput`
- `interface ConversionOutput`
- `interface HistoryEntry` — A single conversion stored in browser history.
- `type Locale`
- `type TransliterationScheme` — Types shared across all layers (core, services, ui).
- `interface UserDictionaryEntry` — A user-supplied Latin → Arabic override.

### `src/shared/utils.ts`

Pure utility functions with no DOM or Node.

- `function clamp(value: number, min: number, max: number): number` — Clamp a number between min and max (inclusive).
- `function containsArabic(text: string): boolean` — Returns true if the string contains at least one Arabic Unicode character (basic Arabic block U+0600–U+06FF).
- `function containsTransliterationDiacritics(text: string): boolean` — Returns true if the string contains diacritic characters commonly used in Brill/ALA-LC transliteration (macrons, underdots, ʿayn, hamza marks).
- `function generateId(): string` — Pure utility functions with no DOM or Node.
- `function nowIso(): string` — Returns an ISO 8601 timestamp string for the current moment.
- `function stripHarakat(text: string): string` — Strip Arabic diacritics (harakat, U+064B–U+0652) from an Arabic string.
- `function truncate(text: string, maxLength: number, suffix?): string` — Truncate a string to `maxLength` characters, appending `suffix` if truncation occurred.

### `src/ui/components/AdvancedOptions.tsx`

- `function AdvancedOptions({ strings: s, options, onChange }: Props)`

### `src/ui/components/Footer.tsx`

- `function Footer({ strings, setView }: FooterProps)`

### `src/ui/components/Header.tsx`

- `function Header({ view, setView, strings, onToggleLocale }: HeaderProps)`

### `src/ui/components/VirtualKeyboard.tsx`

- `type KeyboardLayout`
- `function VirtualKeyboard({
targetRef,
value,
onChange,
layout,
onLayoutChange,
latinTabLabel,
dinTabLabel,
arabicTabLabel
}: VirtualKeyboardProps)`

### `src/ui/hooks/useLocale.ts`

- `function useLocale(): UseLocaleResult`
- `interface UseLocaleResult`

### `src/ui/i18n/ar.ts`

- `const ar: UIStrings`

### `src/ui/i18n/en.ts`

- `const en: UIStrings`

### `src/ui/i18n/index.ts`

- `function applyToDocument(locale?: Locale): void` — Apply dir/lang attributes to <html>.
- `function getLocale(): Locale` — Get the current locale.
- `function initLocale(): Locale` — Initialise locale on page load.
- `function setLocale(locale: Locale): void` — Switch locale and update the <html> element.
- `function subscribeLocale(listener: () => void): () => void` — Subscribe to locale changes.
- `function t(): UIStrings` — Get the UI strings for the current locale.
- `function tKey(key: string): string` — Get a nested string by dot-separated key (e.
- `function toggleLocale(): Locale` — Toggle between 'ar' and 'en'.
- `interface UIStrings` — Full bilingual string contract.

### `src/ui/i18n/types.ts`

Full bilingual string contract.

- `interface UIStrings` — Full bilingual string contract.

### `src/ui/pages/BatchPage.tsx`

- `function BatchPage({ strings: s }: Props)`

### `src/ui/pages/ConverterPage.tsx`

- `function ConverterPage({ strings: s }: Props)`

### `src/ui/pages/DictionaryPage.tsx`

- `function DictionaryPage({ strings: s }: Props)`

### `src/ui/pages/MethodologyPage.tsx`

- `function MethodologyPage({ strings: s }: Props)`

### `src/App.tsx`

- `function App()`
- `type View`

### `src/main.tsx`

(no exports)

### `src/vite-env.d.ts`

(no exports)

### `tests/core/alifWasla.test.ts`

(no exports)

### `tests/core/arabicHouseStyle.test.ts`

(no exports)

### `tests/core/arabicToLatin.test.ts`

(no exports)

### `tests/core/bracketSanitizer.test.ts`

(no exports)

### `tests/core/brillEngineParity.test.ts`

(no exports)

### `tests/core/commonNameAliases.test.ts`

(no exports)

### `tests/core/curlyQuoteNormalization.test.ts`

(no exports)

### `tests/core/din31635.test.ts`

(no exports)

### `tests/core/divineNames.test.ts`

(no exports)

### `tests/core/muslimHeritageFixtures.test.ts`

(no exports)

### `tests/core/orthographyNormalization.test.ts`

(no exports)

### `tests/core/transliteration.test.ts`

(no exports)

### `tests/core/transliterationCorrections.test.ts`

(no exports)

### `tests/services/batchService.test.ts`

(no exports)

### `tests/services/dictionaryService.test.ts`

(no exports)

### `tests/services/historyService.test.ts`

(no exports)

### `tests/services/transliterationService.test.ts`

(no exports)

### `scripts/find-degraded-transliterations.ts`

(no exports)

### `scripts/generate-project-tree.ts`

- `function generate(rootDir: string): string`
- `function generateFormatted(rootDir: string): Promise<string>`
- `function isCurrent(rootDir: string, outputPath: string): Promise<boolean>`

### `scripts/release.ts`

(no exports)
